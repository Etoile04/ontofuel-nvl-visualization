; NFM-228 CI infra: @neo4j-nvl/react@1.1.0 has a peer constraint
; (react 18.0.0 || ^19.0.0) that conflicts with the pinned react@18.2.0.
; The project has always been installed this way; commit it so CI `npm ci`
; resolves consistently instead of hitting ERESOLVE.
legacy-peer-deps=true
